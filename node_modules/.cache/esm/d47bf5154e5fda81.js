"main";let program;_4e5‍.w("commander",[["default",["program"],function(v){program=v}]]);let path;_4e5‍.w("path",[["default",["path"],function(v){path=v}]]);let fs;_4e5‍.w("fs",[["default",["fs"],function(v){fs=v}]]);let prompts;_4e5‍.w("prompts",[["default",["prompts"],function(v){prompts=v}]]);let download;_4e5‍.w("download",[["default",["download"],function(v){download=v}]]);let sprintf;_4e5‍.w("sprintf-js",[["sprintf",["sprintf"],function(v){sprintf=v}]]);let fetch;_4e5‍.w("node-fetch",[["default",["fetch"],function(v){fetch=v}]]);







_4e5‍.g.console.log('Running')
program.version('0.0.1').description('Contact management system')

program
  .command('list')
  .alias('a')
  .description('Add a contact')
  .action(async () => {
    const list = await fetch('https://data.gov.sg/api/action/package_list')
    const listJSON = await list.json()
    listJSON.result.map((obj, idx) => {
      _4e5‍.g.console.log(sprintf('ID: %2.2d Name: %s', idx, obj))
    })
  })

program
  .command('download <package> <dest>')
  .alias('a')
  .description('download')
  .action(async (pkg, dest) => {
    const packageUrl = `https://data.gov.sg/api/action/package_show?id=${pkg}`
    const packageResult = await fetch(packageUrl)
    _4e5‍.g.console.log('Result: ', packageResult)
    const packageJSON = await packageResult.json()
    const {
      result: { resources },
    } = packageJSON
    _4e5‍.g.console.log(`There are ${resources.length} resources`)
    resources.map((obj, idx) => {
      _4e5‍.g.console.log(`${idx} - ${obj.name}`)
    })
    const userResponse = await prompts({
      type: 'number',
      name: 'value',
      message: 'Which file you want to download?',
      validate: value => (value > resources.length ? 'Out of range' : true),
    })
    _4e5‍.g.console.log('Response: ', userResponse)
    let { value } = userResponse
    let { format, name, url } = resources[value]
    _4e5‍.g.console.log('Url: ', url)
    const urlSplit = url.split('.')
    let ext = urlSplit[urlSplit.length - 1]
    if (format == 'API') {
      ext = 'json'
    }
    const filePath = path.join(path.resolve(dest), name, '.', ext)
    _4e5‍.g.console.log('Filepath: ', filePath)
    download(url).pipe(fs.createWriteStream(filePath))
  })

program.parse(process.argv)

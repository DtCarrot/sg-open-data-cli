"main";let program;_feb‍.w("commander",[["default",["program"],function(v){program=v}]]);let path;_feb‍.w("path",[["default",["path"],function(v){path=v}]]);let fs;_feb‍.w("fs",[["default",["fs"],function(v){fs=v}]]);let prompts;_feb‍.w("prompts",[["default",["prompts"],function(v){prompts=v}]]);let download;_feb‍.w("download",[["default",["download"],function(v){download=v}]]);let sprintf;_feb‍.w("sprintf-js",[["sprintf",["sprintf"],function(v){sprintf=v}]]);let fetch;_feb‍.w("node-fetch",[["default",["fetch"],function(v){fetch=v}]]);







_feb‍.g.console.log('Running')
program.version('0.0.1').description('Contact management system')

program
  .command('list')
  .alias('a')
  .description('Add a contact')
  .action(async () => {
    const list = await fetch('https://data.gov.sg/api/action/package_list')
    const listJSON = await list.json()
    listJSON.result.map((obj, idx) => {
      _feb‍.g.console.log(sprintf('ID: %2.2d Name: %s', idx, obj))
    })
  })

program
  .command('download <package> <dest>')
  .alias('a')
  .description('download')
  .action(async (pkg, dest) => {
    const packageUrl = `https://data.gov.sg/api/action/package_show?id=${pkg}`
    const packageResult = await fetch(packageUrl)
    _feb‍.g.console.log('Result: ', packageResult)
    const packageJSON = await packageResult.json()
    const {
      result: { resources },
    } = packageJSON
    _feb‍.g.console.log(`There are ${resources.length} resources`)
    resources.map((obj, idx) => {
      _feb‍.g.console.log(`${idx} - ${obj.name}`)
    })
    const userResponse = await prompts({
      type: 'number',
      name: 'value',
      message: 'Which file you want to download?',
      validate: value => (value > resources.length ? 'Out of range' : true),
    })
    _feb‍.g.console.log('Response: ', userResponse)
    let { value } = userResponse
    let { format, name, url } = resources[value]
    _feb‍.g.console.log('Url: ', url)
    const urlSplit = url.split('.')
    let ext = urlSplit[urlSplit.length - 1]
    if (format == 'API') {
      ext = '.json'
    }
    const filePath = path.join(path.resolve(dest), name, '.', ext)
    _feb‍.g.console.log('Filepath: ', filePath)
    download(url).pipe(fs.createWriteStream(filePath))
  })

program.parse(process.argv)
